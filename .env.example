# Copy to .env for local `netlify dev` / `vercel dev` only.
# .env is gitignored — never commit a real key.
GEMINI_API_KEY=your-key-here
# GEMINI_MODEL=gemini-3-flash-preview
# ALLOWED_ORIGINS=https://narekohanyan.com,http://localhost:4173
